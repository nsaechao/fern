// This file was auto-generated by Fern from our API Definition.

package shifts

import (
	context "context"
	fern "github.com/mixed-file-directory/fern"
	core "github.com/mixed-file-directory/fern/core"
	labor "github.com/mixed-file-directory/fern/labor"
	option "github.com/mixed-file-directory/fern/option"
	http "net/http"
)

type Client struct {
	baseURL string
	caller  *core.Caller
	header  http.Header
}

func NewClient(opts ...option.RequestOption) *Client {
	options := core.NewRequestOptions(opts...)
	return &Client{
		baseURL: options.BaseURL,
		caller: core.NewCaller(
			&core.CallerParams{
				Client:      options.HTTPClient,
				MaxAttempts: options.MaxAttempts,
			},
		),
		header: options.ToHeader(),
	}
}

// Creates a new `Shift`.
//
// A `Shift` represents a complete workday for a single employee.
// You must provide the following values in your request to this
// endpoint:
//
// - `location_id`
// - `employee_id`
// - `start_at`
//
// An attempt to create a new `Shift` can result in a `BAD_REQUEST` error when:
//
//   - The `status` of the new `Shift` is `OPEN` and the employee has another
//     shift with an `OPEN` status.
//   - The `start_at` date is in the future.
//   - The `start_at` or `end_at` date overlaps another shift for the same employee.
//   - The `Break` instances are set in the request and a break `start_at`
//     is before the `Shift.start_at`, a break `end_at` is after
//     the `Shift.end_at`, or both.
func (c *Client) Create(
	ctx context.Context,
	request *labor.CreateShiftRequest,
	opts ...option.RequestOption,
) (*fern.CreateShiftResponse, error) {
	options := core.NewRequestOptions(opts...)

	baseURL := "https://connect.squareupsandbox.com"
	if c.baseURL != "" {
		baseURL = c.baseURL
	}
	if options.BaseURL != "" {
		baseURL = options.BaseURL
	}
	endpointURL := baseURL + "/v2/labor/shifts"

	headers := core.MergeHeaders(c.header.Clone(), options.ToHeader())

	var response *fern.CreateShiftResponse
	if err := c.caller.Call(
		ctx,
		&core.CallParams{
			URL:         endpointURL,
			Method:      http.MethodPost,
			MaxAttempts: options.MaxAttempts,
			Headers:     headers,
			Client:      options.HTTPClient,
			Request:     request,
			Response:    &response,
		},
	); err != nil {
		return nil, err
	}
	return response, nil
}

// Returns a paginated list of `Shift` records for a business.
// The list to be returned can be filtered by:
//
// - Location IDs.
// - Employee IDs.
// - Shift status (`OPEN` and `CLOSED`).
// - Shift start.
// - Shift end.
// - Workday details.
//
// The list can be sorted by:
//
// - `start_at`.
// - `end_at`.
// - `created_at`.
// - `updated_at`.
func (c *Client) Search(
	ctx context.Context,
	request *labor.SearchShiftsRequest,
	opts ...option.RequestOption,
) (*fern.SearchShiftsResponse, error) {
	options := core.NewRequestOptions(opts...)

	baseURL := "https://connect.squareupsandbox.com"
	if c.baseURL != "" {
		baseURL = c.baseURL
	}
	if options.BaseURL != "" {
		baseURL = options.BaseURL
	}
	endpointURL := baseURL + "/v2/labor/shifts/search"

	headers := core.MergeHeaders(c.header.Clone(), options.ToHeader())

	var response *fern.SearchShiftsResponse
	if err := c.caller.Call(
		ctx,
		&core.CallParams{
			URL:         endpointURL,
			Method:      http.MethodPost,
			MaxAttempts: options.MaxAttempts,
			Headers:     headers,
			Client:      options.HTTPClient,
			Request:     request,
			Response:    &response,
		},
	); err != nil {
		return nil, err
	}
	return response, nil
}

// Returns a single `Shift` specified by `id`.
func (c *Client) Get(
	ctx context.Context,
	// The UUID for the `Shift` being retrieved.
	id string,
	opts ...option.RequestOption,
) (*fern.GetShiftResponse, error) {
	options := core.NewRequestOptions(opts...)

	baseURL := "https://connect.squareupsandbox.com"
	if c.baseURL != "" {
		baseURL = c.baseURL
	}
	if options.BaseURL != "" {
		baseURL = options.BaseURL
	}
	endpointURL := core.EncodeURL(baseURL+"/v2/labor/shifts/%v", id)

	headers := core.MergeHeaders(c.header.Clone(), options.ToHeader())

	var response *fern.GetShiftResponse
	if err := c.caller.Call(
		ctx,
		&core.CallParams{
			URL:         endpointURL,
			Method:      http.MethodGet,
			MaxAttempts: options.MaxAttempts,
			Headers:     headers,
			Client:      options.HTTPClient,
			Response:    &response,
		},
	); err != nil {
		return nil, err
	}
	return response, nil
}

// Updates an existing `Shift`.
//
// When adding a `Break` to a `Shift`, any earlier `Break` instances in the `Shift` have
// the `end_at` property set to a valid RFC-3339 datetime string.
//
// When closing a `Shift`, all `Break` instances in the `Shift` must be complete with `end_at`
// set on each `Break`.
func (c *Client) Update(
	ctx context.Context,
	// The ID of the object being updated.
	id string,
	request *labor.UpdateShiftRequest,
	opts ...option.RequestOption,
) (*fern.UpdateShiftResponse, error) {
	options := core.NewRequestOptions(opts...)

	baseURL := "https://connect.squareupsandbox.com"
	if c.baseURL != "" {
		baseURL = c.baseURL
	}
	if options.BaseURL != "" {
		baseURL = options.BaseURL
	}
	endpointURL := core.EncodeURL(baseURL+"/v2/labor/shifts/%v", id)

	headers := core.MergeHeaders(c.header.Clone(), options.ToHeader())

	var response *fern.UpdateShiftResponse
	if err := c.caller.Call(
		ctx,
		&core.CallParams{
			URL:         endpointURL,
			Method:      http.MethodPut,
			MaxAttempts: options.MaxAttempts,
			Headers:     headers,
			Client:      options.HTTPClient,
			Request:     request,
			Response:    &response,
		},
	); err != nil {
		return nil, err
	}
	return response, nil
}

// Deletes a `Shift`.
func (c *Client) Delete(
	ctx context.Context,
	// The UUID for the `Shift` being deleted.
	id string,
	opts ...option.RequestOption,
) (*fern.DeleteShiftResponse, error) {
	options := core.NewRequestOptions(opts...)

	baseURL := "https://connect.squareupsandbox.com"
	if c.baseURL != "" {
		baseURL = c.baseURL
	}
	if options.BaseURL != "" {
		baseURL = options.BaseURL
	}
	endpointURL := core.EncodeURL(baseURL+"/v2/labor/shifts/%v", id)

	headers := core.MergeHeaders(c.header.Clone(), options.ToHeader())

	var response *fern.DeleteShiftResponse
	if err := c.caller.Call(
		ctx,
		&core.CallParams{
			URL:         endpointURL,
			Method:      http.MethodDelete,
			MaxAttempts: options.MaxAttempts,
			Headers:     headers,
			Client:      options.HTTPClient,
			Response:    &response,
		},
	); err != nil {
		return nil, err
	}
	return response, nil
}
